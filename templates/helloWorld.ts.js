// additional info for templates like copy extra libraries.
// in this case library needs JSON
const config = {
  files: [""],
  commands: ["npm i @types/node"],
  repos: [""],
  descriptions: [""]
};

module.exports = config;
